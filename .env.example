# Copy to .env.local and fill in. .env.local is git-ignored — never commit it.

# ---------------------------------------------------------------------------
# BRAIN — which backend answers.
#   stepfun  : remote StepFun (step-3.7-flash)
#   llamacpp : local llama-server, GGUF on CUDA (no key needed)
#   openai   : any other OpenAI-compatible endpoint
# All three speak /v1/chat/completions, so switching is config-only.
# ---------------------------------------------------------------------------
BRAIN_SERVICE=stepfun

# Remote backend
BRAIN_API_KEY=
BRAIN_API_ENDPOINT=https://api.stepfun.ai/v1/chat/completions
BRAIN_MODEL=step-3.7-flash
BRAIN_TIMEOUT_MS=30000

# Local llama.cpp backend (used directly, or as automatic fallback)
LLAMA_SERVER_HOST=http://localhost:8080
BRAIN_MODEL_LOCAL=mother-local

# ---------------------------------------------------------------------------
# EARS / TURN / MOUTH
# ---------------------------------------------------------------------------
EARS_MODEL=large-v3-turbo
EARS_LANGUAGE=fr
TURN_SILENCE_MS=700
MOUTH_VOICE=/workspace/models/piper/fr_FR-siwis-medium.onnx

# ---------------------------------------------------------------------------
# Other services (optional)
# ---------------------------------------------------------------------------
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
