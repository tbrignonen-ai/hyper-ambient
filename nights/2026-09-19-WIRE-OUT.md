---
date: 2026-09-19
heure: ~20:35 Europe/Paris
type: out
lane: WIRE
cible: Claude (lead technique) + OC (deadline 20:50)
auteur: Cursor (Grok 4.6) — session desktop
related: ["[[2026-09-19-P0-2-CARTE-OUT]]", "[[2026-09-19-C3-OUT]]", "[[2026-09-19-C11-OUT]]", "[[2026-09-19-CLAUDE-POUR-GROK-SUITE]]"]
deadline: 20:50 Europe/Paris
---

# WIRE OUT — JeV + calculer + hotwords branchés au host-agent

Thomas : **finir max ce soir**. Pas de commit. Pas de relance live (stack dégustation encore via `/tmp/relance_hostagent.sh`). Aucune valeur de secret.

P0-2 / C4 / C8 ont déjà leurs OUT. Cette lane **branche** ce qui était prêt sur disque et n'était pas dans `serve_hostagent.py`.

## Cause racine (avant ce tour)

| Module | État avant |
|---|---|
| C3 `JevReflexe` | Module + 6 tests, **aucun appel** depuis le tour vocal |
| C12 `calculer` | Module + tests, **absent** du registre host-agent (déjà corrigé en parallèle, re-vérifié ici) |
| C11 identité | Prompt déjà injecté dans `LlamaCppBrain` |
| Hotwords Whisper | Banc OK ; câblage EARS livré P0-2, re-vérifié ici |
| `TYPESAFE_API_KEY` | Dans `.env.local` possible, **ignorée** par `charger_env_local` |

## Diff ce tour

| Fichier | Changement |
|---|---|
| `dev/scripts/serve_hostagent.py` | `jev_ignore_tour()` ; `JevReflexe` au `load` ; ignore le tour si JeV tranche « pas adressée à MOTHER » ; `TYPESAFE_API_KEY` dans la whitelist C1 |
| `dev/tests/test_jev_branchement.py` | 5 tests TDD |
| `dev/scripts/relance_hostagent.sh` | exporte `TYPESAFE_API_KEY` (longueur seulement, jamais la valeur) |
| `src/brain/tools_web.py` | `DuckDuckGoSearch.__call__` (test C4 rouge : objet non appelable) |
| `src/i18n/__init__.py` | `tools.calculer` FR/EN |

Règle produit respectée : `named_harness` est **observation** (log « aucun envoi »). Aucun modèle n'envoie une tâche à un harnais. Sans clé / timeout / erreur → `None` → **repli bouton** (le tour continue).

## TDD rouge → vert (JeV)

**Rouge** (fonction absente) :

```
$ python -m pytest -q dev/tests/test_jev_branchement.py --tb=short
FFFFF                                                                    [100%]
AttributeError: module 'serve_hostagent_env_local' has no attribute 'jev_ignore_tour'
AssertionError: assert 'TYPESAFE_API_KEY' in ['CODEX_BRIDGE_TOKEN']
5 failed in 0.20s
```

**Vert** (sprint lanes, Windows hôte) :

```
$ python -m pytest -q dev/tests/test_jev_branchement.py dev/tests/test_carte_figee.py dev/tests/test_ears_hotwords.py dev/tests/test_tools_calculator.py dev/tests/test_tools_web.py dev/tests/test_c8_i18n.py dev/tests/test_hostagent_env_local.py dev/tests/test_jev_reflexe.py --tb=line
..............................................................           [100%]
62 passed in 0.88s
```

## Comportement attendu après relance (non faite)

1. Boot : `JEV   : réflexe en entrée (repli silencieux sans clé)` + `OUTILS: …, calculer, …`
2. Sans `TYPESAFE_API_KEY` : inchangé (bouton).
3. Avec clé : phrase non adressée à MOTHER → tour ignoré, pas de voix.
4. Harnais nommé dans la phrase → log observation, **pas** d'appel pont.

Relance officielle, **feu vert Claude** seulement :

```
docker exec mother-core-dev bash /workspace/dev/scripts/relance_hostagent.sh
```

## Non fait

- Relance live + preuve boot (C13 pythonw toujours ouvert)
- P0-1 copie modèles vers `E:`
- Commit / push

## Done

| Attendu | Statut |
|---|---|
| JeV branché host-agent | **oui** (repli silencieux) |
| `calculer` au registre | **oui** (re-vérifié) |
| Hotwords EARS | **oui** (P0-2, re-vérifié) |
| Identité C11 | déjà dans `openai_compat` |
| pytest collé | **oui**, 62 verts |
| Relance live | **non** |
| Commit | **non** |
