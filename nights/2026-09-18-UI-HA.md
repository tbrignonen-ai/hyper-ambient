---
date: 2026-09-18
type: out
auteur: Cursor (Grok exécutant)
cible: Thomas + OC + Claude
statut: livré localement — pas de push
surface: native/presence uniquement
related:
  - "[[2026-09-18-BRIEF-CURSOR]]"
  - "[[2026-09-18-ASSIGN-SOIR]]"
  - "[[2026-09-17-CURSOR-UI-HA]]"
---

# Cursor — UI Hyper Ambient 18 sept (L3)

Mission : **1 geste HA réel** (nappe / orbe lisible live). Thomas 17 : UI trop faible, surtout le transparent. Garder wizard 3 étapes FR + éclair distant. Hors périmètre : pas docker, Hermes, secrets, brain, TTS. Pas de push. L2 voix non touchée (P0 = L3).

## Fait

Le chroma-key de la fenêtre **app** (17 sept) est retiré. Le bureau ne perce plus le panneau : champ sombre plein, texte lisible. Le geste HA n’est plus un trou — c’est une **nappe remplie** qui dérive dans la marge autour de la vitre, plus un **souffle** (anneaux qui s’éloignent du cœur) sur l’orbe.

L’overlay flottant garde le chroma-key **sur les coins seulement**. Les pixels dessinés restent opaques (`-alpha` 1,0). Plus de bulle fantôme à 0,16.

Wizard **toujours 3 étapes skippables**, libellés **FR only**. Éclair 15 sept **intact** : éteint = « Modèle local » ; allumé seulement en `escalade` = « Appel distant » + statut en clair.

## Chemins

- `native/presence/overlay.py` — `dessiner_souffle` (geste live), nappe à corps rempli, palettes plus lisibles au repos, overlay opaque.
- `native/presence/app.py` — plus de `transparentcolor` ; vitre insete (`MARGE_NAPPE`) pour laisser la nappe visible ; pied de wizard pour que Passer / Continuer restent cliquables.
- `native/presence/onboarding.py` — « La lumière respire avec elle. » ; 3 étapes + éclair inchangés.
- `dev/tests/test_presence_onboarding.py` — nappe dans la marge, souffle, lock FR, pas de chroma-key app.

Aucun fichier hostagent / brain / TTS / docker modifié.

## Captures

Coffre + repo `nights/` :

| Fichier | Quoi |
|---|---|
| `2026-09-18-cursor-ui-1-bienvenue.png` | Étape 1/3, nappe en marge, orbe + souffle, Continuer / Passer |
| `2026-09-18-cursor-ui-2-ptt.png` | Étape 2/3, Espace / Ctrl + Espace, essai, Passer visible |
| `2026-09-18-cursor-ui-3-masquage.png` | Étape 3/3, Commencer / Commencer et masquer |
| `2026-09-18-cursor-ui-4-local.png` | App : nappe, orbe, éclair éteint « Modèle local » |
| `2026-09-18-cursor-ui-5-parole.png` | Parole : orbe chaud, éclair éteint |
| `2026-09-18-cursor-ui-6-distant.png` | Escalade : nappe braise, éclair allumé, « Appel distant » |
| `2026-09-18-cursor-ui-7-overlay-parole.png` | Overlay corps rempli + souffle (coins percés) |
| `2026-09-18-cursor-ui-8-overlay-escalade.png` | Overlay + éclair pendant l’escalade |
| `2026-09-18-cursor-ui-9-overlay-repos.png` | Overlay au repos, encore lisible |

Script de prise : `nights/_capture_ui_ha_2026-09-18.py` (session vocale volontairement non lancée).

## Tests

```
python -m pytest dev/tests/test_presence_onboarding.py dev/tests/test_presence_interruption.py -q
................s........                                                         [100%]
24 passed, 1 skipped in 1.17s
```

(le skip = premier `Tk()` parfois sans `init.tcl` sous Python 3.13 ; les tests Tk suivants passent.)

```
python -m py_compile native/presence/app.py native/presence/overlay.py native/presence/onboarding.py
```

Revue visuelle : captures ci-dessus (app opaque + nappe en marge ; overlay opaque sur la forme).

## Pas fait

- L2 voix (si temps) — non attaquée, P0 = L3.
- Push GitHub — fin de lot Claude / go Thomas.
- Coins de vitre arrondis (Tk ne le fait pas nativement).
