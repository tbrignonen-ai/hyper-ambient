---
date: 2026-09-19
heure: ~20:15 Europe/Paris
type: out
lane: C8
cible: OC (push 20:50) + Claude WRAP
auteur: Cursor (Grok 4.6) — LAUNCH-C8.ps1
---

# C8 OUT — English 0.1 (FR reste le défaut)

Thomas absent. **Pas de commit.** `.env.local` non touché. `magpie_tts.py` non touché.
Prompt CLI tronqué à la 1re ligne (deadline) ; lane identifiée via l’arbre de process `LAUNCH-C8.ps1`.

Bascule : `HA_LANG=en` ou `HYPER_AMBIENT_LANG=en` / `en-US`. Toute autre valeur → `fr`.

## TDD

**Rouge** (module absent) :

```
$ python -m pytest -q dev/tests/test_c8_i18n.py
ERROR collect — ModuleNotFoundError: No module named 'src.i18n'
1 error in 0.11s
```

**Vert** :

```
$ python -m pytest -q dev/tests/test_c8_i18n.py
.............                                                            [100%]
13 passed in 0.03s
```

Régression FR (défaut) :

```
$ python -m pytest -q dev/tests/test_c11_identity.py dev/tests/test_normalize_nombres.py dev/tests/test_jev_reflexe.py
..............                                                           [100%]
14 passed in 0.93s (avec C8 : 27 passed)

$ python -m pytest -q dev/tests/test_presence_onboarding.py::test_copies_onboarding_restent_en_francais \
    dev/tests/test_presence_onboarding.py::test_wizard_et_eclair_distant_sont_visibles
..                                                                       [100%]
2 passed in 0.20s
```

Annonces outils FR inchangées (`annonce_outil("ask_codex")` = phrase actuelle). Les tests async `test_outils_voix` échouent déjà sans `pytest-asyncio` (hors C8).

## Diff résumé

| Fichier | Rôle |
|---|---|
| `src/i18n/__init__.py` | catalogues FR/EN, `langue()`, `t()`, `ui()`, `system_prompt()`, `questions_jev()`, critères JeV EN |
| `src/brain/local_prompt.py` | `LOCAL_SYSTEM_PROMPT_EN` (identité Hyper Ambient) |
| `src/brain/openai_compat.py` | injecte `system_prompt()` selon `HA_LANG` |
| `src/mouth/normalize.py` | chemin EN (`3:30` / `15h30`, `%`, décimaux) ; FR inchangé par défaut |
| `src/ears/jev_reflexe.py` | POST JeV utilise `_questions_pour_appel()` (mêmes 13 id) |
| `dev/scripts/serve_hostagent.py` | `annonce_outil()` lit `src.i18n` |
| `native/presence/onboarding.py` | `ui_presence()`, `raccourcis_lisibles()`, libellés éclair/statut |
| `native/presence/app.py` | wizard + bouton Talk/Parler localisés |
| `dev/tests/test_c8_i18n.py` | 13 tests |
| `README.en.md` | notice produit EN 0.1 + carte figée |

Non touché : `src/mouth/magpie_tts.py`, EARS ASR, `.env.local`, Docker.

Magpie verbalise les nombres via `nombres_en_lettres()` : `HA_LANG=en` suffit, sans éditer Magpie.

## Push (cette lane)

```
git add src/i18n README.en.md
git add src/brain/local_prompt.py src/brain/openai_compat.py
git add src/mouth/normalize.py src/ears/jev_reflexe.py
git add native/presence/onboarding.py native/presence/app.py
git add dev/scripts/serve_hostagent.py
git add dev/tests/test_c8_i18n.py
git add nights/2026-09-19-C8-EN-OUT.md
```

`serve_hostagent.py`, `app.py`, `jev_reflexe.py`, `openai_compat.py` sont **partagés** (WIRE / C13 / C1). OC : un seul add final, pas un commit C8 isolé si ces fichiers ont encore bougé après 20:15.

## Hors 0.1

- Pas de locale `en` persistée dans Presence `presence.json` (env seulement).
- Statut WS « Canal prêt. Maintenez Parler… » encore FR (fil SessionVocale).
- README.md dev (make/docker) déjà en anglais ; `README.en.md` = notice produit 0.1.
