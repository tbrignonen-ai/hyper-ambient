---
date: 2026-09-19
type: out
lane: C1
auteur: Cursor (Grok 4.6)
plan: "[[2026-09-19-BRIEF-CURSOR]]"
arbitrage: "[[2026-09-19-CLAUDE-ARBITRAGE-LANES]]"
---

# C1 OUT — Outils câblés au host-agent

**Relance host-agent / `:8090` : non faite** (arbitrage Claude : GPU partagé avec la dégustation, feu vert requis). Correctif + tests + preuve registre hors process live = faits. Preuve boot + tour réel = en attente du créneau Claude.

## 1. Cause racine (prouvée avant correctif)

Le host-agent tourne **dans** `mother-core-dev` (Up 3 h, CID `4343886a4552`), PID `192` :

```
184 bash -lc cd /workspace && python3 dev/scripts/serve_hostagent.py > /workspace/logs/hostagent-19.log 2>&1
192 python3 dev/scripts/serve_hostagent.py
```

Pas passé par `dev/scripts/relancer_routeur.sh` (seul script qui lisait `.env.local` et exportait les jetons, `tr -d '\015'`).

`serve_hostagent.py` ne lisait que `os.getenv`. `docker start` ne recharge pas `env_file`. Les clés outils sont donc **absentes du process**, alors qu'elles sont **présentes et non vides** dans le fichier monté `/workspace/.env.local`.

`.env.local` (lecture seule, valeurs jamais affichées) :

| Clé | Fichier | Process host-agent (PID 192) | Ligne |
|---|---|---|---|
| `CODEX_BRIDGE_TOKEN` | present, 64 car. | **ABSENT** | LF |
| `CLI_BRIDGE_TOKEN` | present, 40 car. | **ABSENT** | **CRLF** |
| `SEARXNG_URL` | present, 32 car. | **ABSENT** | LF |
| `TAVILY_API_KEY` | ABSENT | ABSENT | — |
| `MUSE_BRIDGE_URL` | ABSENT | ABSENT | — |

Fins de ligne du fichier : **mixtes** (2 CRLF / 81 LF). Les deux CRLF sont `CLI_BRIDGE_TOKEN` et `CLI_BRIDGE_URL` — piste 401 si un chargeur naïf colle `\r` au jeton. Ici le symptôme live est l'**absence**, pas le `\r` : Docker n'a même pas injecté les clés.

Log boot **courant** (`/workspace/logs/hostagent-19.log`, pas `/tmp/hostagent.log` qui est **périmé**) :

```
MOUTH : post-gain sortie +0.0 dB (x1.00), limiteur doux tanh
EARS  : chargement faster-whisper / large-v3-turbo sur cuda…
BRAIN : router @ reflex=http://localhost:8080/v1/chat/completions deep=https://api.commandcode.ai/provider/v1/chat/completions — reflex HTTP 200, deep HTTP 200
OUTILS: aucun backend configure — tour de parole sans outil
MOUTH : chargement pocket-tts french_24l / estelle profil=aurora device=cpu demi_tons=+0…
écoute sur 0.0.0.0:8001 /hostagent
```

`/tmp/hostagent.log` (ancien `relancer_routeur`) dit encore `OUTILS: ask_codex, web_search` — ce n'est **pas** le process du jour.

## 2. Diff résumé

Fichiers touchés (périmètre C1) :

- `dev/scripts/serve_hostagent.py` — `parser_env_local` / `charger_env_local` (stdlib, strip `\r`, **whitelist outils seulement**, ignore `MOUTH_*` / `EARS_*` / `BRAIN_*` / `MODEL`). Appelé au début de `main()` et de `HostPipeline.load()`. `construire_registre` strippe aussi les jetons lus.
- `dev/tests/test_hostagent_env_local.py` — 6 tests (CRLF, vide déjà posé, non-écrasement, clés modèle, whitelist, registre après chargement).

Non touchés : `.env.local`, `relancer_routeur.sh` (déjà correct, mais non utilisé au boot du jour), `tools_muse.py`, GPU, compose.

## 3. Test rouge → vert

**Rouge** (fonction absente, 5 tests — avant correctif) :

```
$ docker exec -w /workspace mother-core-dev python3 -m pytest -q dev/tests/test_hostagent_env_local.py
FFFFF                                                                    [100%]
AttributeError: module 'serve_hostagent_env_local' has no attribute 'charger_env_local'
5 failed in 0.65s
```

Le 5e test a d'abord vérifié `len(construire_registre(...)) == 0` **avant** l'appel au chargeur : registre vide sans propagation, c'est le bug live.

**Rouge bis** (whitelist, après un premier chargeur trop large qui injectait `HF_TOKEN` / `ALIAS`) :

```
$ docker exec -w /workspace mother-core-dev python3 -m pytest -q \
    dev/tests/test_hostagent_env_local.py::test_env_local_n_injecte_que_les_cles_outils
F
AssertionError: Left contains 2 more items: {'ALIAS': ..., 'HF_TOKEN': ...}
1 failed in 0.49s
```

**Vert** :

```
$ docker exec -w /workspace mother-core-dev python3 -m pytest -q \
    dev/tests/test_hostagent_env_local.py dev/tests/test_outils_voix.py
.....................................                                    [100%]
37 passed in 0.55s
```

## 4. Preuve registre sans relance (process jetable, pas le host-agent live)

Même état que le boot du jour (clés absentes dans ce process) → `charger_env_local()` → registre :

```
avant: CODEX_BRIDGE_TOKEN=absent CLI_BRIDGE_TOKEN=absent SEARXNG_URL=absent TAVILY_API_KEY=absent MUSE_BRIDGE_URL=absent
registre_avant: []
injectees: ['CLI_BRIDGE_TOKEN', 'CLI_BRIDGE_URL', 'CODEX_BRIDGE_TOKEN', 'CODEX_BRIDGE_URL', 'SEARXNG_URL']
apres: CODEX_BRIDGE_TOKEN=present CLI_BRIDGE_TOKEN=present SEARXNG_URL=present TAVILY_API_KEY=absent MUSE_BRIDGE_URL=absent
OUTILS: ask_claude, ask_codex, web_search
```

Le process live PID 192 **n'a pas** reçu ces variables (pas de relance). Au prochain boot, le log attendu est :

```
ENV   : CLI_BRIDGE_TOKEN, CLI_BRIDGE_URL, CODEX_BRIDGE_TOKEN, CODEX_BRIDGE_URL, SEARXNG_URL depuis .env.local
OUTILS: ask_claude, ask_codex, web_search — porte en mode auto
```

(`ask_muse` restera absent : `MUSE_BRIDGE_URL` vide/absent, voulu.)

## 5. Preuve bout-en-bout (tour réel) — BLOQUÉE

Arbitrage Claude : **ne pas** relancer le host-agent ni `:8090`, ne charger aucun modèle GPU, sans feu vert.

À faire dans le créneau Claude, **sans** `docker compose up` / recreate :

```
docker exec mother-core-dev bash /workspace/dev/scripts/relancer_routeur.sh
# ou tuer PID 192 puis relancer python3 dev/scripts/serve_hostagent.py
# puis : grep OUTILS /workspace/logs/hostagent-*.log
# puis 1 tour (texte ou voix) ask_claude / ask_codex
# option : python3 dev/scripts/verify_hostagent_loop.py
```

`relancer_routeur.sh` **écrase** `MOUTH_*` / `EARS_*` / `BRAIN_*` (Supertonic, Qwen3, MiniMax). Le boot actuel est pocket + faster-whisper + router. Pour ne pas bouger la carte : relancer **uniquement** `python3 dev/scripts/serve_hostagent.py` (le chargeur Python suffit) plutôt que `relancer_routeur.sh`.

## Done C1

| Preuve | Statut |
|---|---|
| Cause racine collée | **oui** |
| Diff résumé | **oui** |
| pytest rouge→vert collé | **oui** |
| Log boot `OUTILS: ask_codex, ask_claude, …` sur le process live | **non** — attend feu vert relance |
| Log d'un tour réel avec outil | **non** — idem |
