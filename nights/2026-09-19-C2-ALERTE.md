---
date: 2026-09-19
type: out
lane: C2
cible: Claude (arbitrage) + OC
auteur: Cursor (Grok 4.6)
---

# C2 OUT — Alerte visible + reprise — 19 sept

Workspace `D:\BGB Training\MOTHER-dev` · branche `nuit/2026-08-27` · **pas de commit**.
Aucun fichier C1 (`serve_hostagent.py`, `src/brain/tools*`). Host-agent / `:8090` **non relancés**, aucun modèle chargé.

## Diagnostic (avant correctif)

| Endpoint | Constat live (lecture seule) | Sémantique sonde |
|---|---|---|
| Camunda `:8088` | **déjà UP** (`orchestration` Up 20 h, `GET /v2/topology` 200, gateway 8.8.35, 1 broker). **Pas** lancé via compose. | HTTP 200 + brokers > 0 |
| Pont Codex `:8765` | Process vivant, `GET` → **501** (méthode non supportée) | toute réponse HTTP = vivant |
| Pont Claude `:8766` | idem **501** | idem |
| Accueil vocal `:8001` | Process vivant, `GET /` → **404** | idem |
| Modèle local `:8090` | `GET /health` **200** | 200 |

Cause racine du trou démo : `handle_health_check` ne sondait **que** la topologie Camunda. Camunda UP masquait un pont coupé (`healthStatus` restait `UP`).

## Composants sondés (après correctif)

1. suivi automatique (Camunda `:8088/v2/topology`) — optionnel ; s'il est down, mode dégradé `worker.py --once` continue en stdlib
2. pont Codex `http://127.0.0.1:8765/ask`
3. pont Claude `http://127.0.0.1:8766/ask`
4. accueil vocal `http://127.0.0.1:8001/`
5. modèle local `http://127.0.0.1:8090/health`

Refus de connexion / timeout = panne. 4xx/5xx = process vivant (les ponts répondent 501 au GET, l'accueil 404).

## 3 messages exacts (FR simple, dossier §10.2)

| Canal | Coupe (DEGRADED, pont Codex) | Reprise (UP) |
|---|---|---|
| Bandeau Presence | `Le pont vers Codex ne répond plus, je continue en local.` | bandeau **disparu** |
| Phrase (à dire) | `Le pont vers Codex ne répond plus, je continue en local.` | `Pont Codex rétabli.` |
| Note Obsidian | même phrase + tableau `pont Codex \| DOWN` | `Statut \| UP`, `Composants en panne \| aucun` |

Contraste bandeau : fond `#7a1212`, texte `#fff8e8`, ratio WCAG **≥ 4.5**. Label `takefocus=1`, rôle `alert`, focus posé à l'apparition (lecteur d'écran / Tab).

## Tests rouge → vert

**Rouge vu** (code actuel, avant correctif) :

```
python -m pytest dev/tests/test_health_sondes.py -q --tb=short
F.F
AssertionError: assert 'UP' == 'DEGRADED'   # Camunda UP masque le pont mort
AssertionError: assert 'pont Codex' in texte
ERROR test_presence_sante.py — ModuleNotFoundError: native.presence.sante
```

**Vert après correctif** :

```
python -m pytest dev/tests/test_health_sondes.py dev/tests/test_presence_sante.py --tb=short
============================= test session starts =============================
platform win32 -- Python 3.13.14, pytest-9.1.1
collected 7 items
dev\tests\test_health_sondes.py ...                                      [ 42%]
dev\tests\test_presence_sante.py ....                                    [100%]
============================== 7 passed in 2.76s ==============================
```

`test_presence_onboarding.py` : 1 flake Tk `tcl_findLibrary` (geste souffle, hors C2) ; **rejoué vert**. Pas de régression bandeau.

## Démo réelle (sans tuer le pont live)

Le pont `:8765` n'a **pas** été arrêté : C1 parallèle + consigne GPU. Coupe **réelle HTTP** : URL Codex pointée sur `127.0.0.1:1` (refus), les autres endpoints restent les vrais.

```
AVANT:   status=UP       degraded=''           ms=41
PENDANT: status=DEGRADED degraded='pont_codex' ms=1560
         alert='Le pont vers Codex ne répond plus, je continue en local.'
APRES:   status=UP       degraded=''           ms=52
```

| Durée | Mesure | Commentaire |
|---|---|---|
| détection → alerte | **41 → 1560 ms** sonde (timeout 1,5 s sur `:1`) + bandeau immédiat à l'`appliquer_etat_sante` | Un **kill réel** du process `:8765` est un refus de connexion, typiquement ≪ 100 ms, puis poll Presence **2 s** |
| relance → retour | **52 ms** sonde live + bandeau retiré | Phrase reprise dans `ligne_etat` |

Note coffre (état final stack sain) : `C:\Users\thoma\obsidian-vault\10-Projects\MOTHER\nights\2026-09-19-HEALTH.md`
(`Statut | UP`, pont Codex UP HTTP 501 = vivant).

Captures Presence (fenêtre réelle, PrintWindow) :

- `nights/2026-09-19-c2-avant.png` — pas de bandeau
- `nights/2026-09-19-c2-pendant.png` — bandeau rouge contrasté, phrase Codex
- `nights/2026-09-19-c2-apres.png` — bandeau disparu

## Diff résumé

- `workers/night_health_vault_note/handlers.py` — sondes nommées, messages FR, snapshot JSON, note avec composants
- `workers/night_health_vault_note/worker.py` — `fetchVariable` étendu + `--once` (mode dégradé sans job Camunda)
- `resources/bpmn/night_health_vault_note.bpmn` — libellé « ponts et services » (même `process id`)
- `native/presence/sante.py` — contrat bandeau sans GUI
- `native/presence/app.py` — bandeau + `--sante` + sondes live via `main()` (`--sondes` auto si pas de fichier)
- tests : `dev/tests/test_health_sondes.py`, `dev/tests/test_presence_sante.py`

Filage jury : `python native/presence/app.py` (sondes toutes les 2 s) + `python workers/night_health_vault_note/worker.py --once` pour la note. Couper le process `:8765`, attendre le bandeau, relancer le pont.

## Besoin vocal — Claude arbitre

**Pas d'entrée existante** sur le host-agent pour faire dire une phrase hors tour PTT (`serve_hostagent.py` : pas de `/announce`, pas de `speak`, non modifié — consigne C2).

Demande : un point d'entrée **hors C2** (une ligne HTTP ou message WS déjà prévu) qui joue exactement :

1. `Le pont vers Codex ne répond plus, je continue en local.`
2. `Pont Codex rétabli.`

sans relancer le host-agent ni recharger `:8090`. En attendant : la phrase est **affichée** (bandeau + note) ; la voie parlée n'est pas branchée.

## Hors périmètre volontaire

- Pont live `:8765` non stoppé (C1 / créneau Claude).
- Pas de `docker compose` / recreate.
- Pas de secret affiché.
