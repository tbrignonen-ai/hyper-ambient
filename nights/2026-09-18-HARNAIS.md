# Harnais L1 — vendredi 18 sept. 2026 (soir)

Mission : ponts Codex :8765 + Claude :8766, `ask_codex` enregistré (brain/hostagent).
Hermes : appel only, AUCUNE config, pas SAWB. Vérifié ~22h35 Paris (20h35 UTC).

## État

- **Pont Codex :8765 — UP, PONG OK** (5,3 s).
- **Pont Claude :8766 — UP, PONG OK** (11,0 s).
- **`ask_codex` enregistré — OUI** : prouvé par enregistrement direct
  (`ask_codex` dans le registre, `danger=read`, schéma exposé).
  Côté hostagent : code vérifié (`construire_registre` + `verifier_registre`
  dans `dev/scripts/serve_hostagent.py`) ; rejouage live impossible ce soir
  (voir blockers).
- **Hermes — appel only, rien configuré** : 1 seul appel, refus propre
  `hermes indisponible` en 0,0 s. `CLI_BRIDGE_HERMES` non armé,
  `ask_hermes` non exposé (refusé par `verifier_registre`, par design).
- **Jetons** : `CODEX_BRIDGE_TOKEN` (64 car.) et `CLI_BRIDGE_TOKEN` (40 car.)
  présents et non vides dans `.env.local` (valeurs jamais affichées).

## Preuves PONG

Sondes HTTP `POST /ask` + Bearer (jeton lu depuis `.env.local`, stripé).

| Sonde | Résultat |
|---|---|
| `:8765 /ask` sans jeton | **401** `{"ok": false, "error": "non autorise"}` |
| `:8765 /inconnu` sans jeton | **404** `{"ok": false, "error": "inconnu"}` |
| `:8765` `{"question":"PONG"}` + jeton | **200 `ok:true` en 5,3 s** : `PONG !` |
| `:8766 /ask` sans jeton | **401** `{"ok": false, "error": "non autorise"}` |
| `:8766` `{"question":"PONG","agent":"claude"}` + jeton | **200 `ok:true` en 11,0 s** : `Pong reçu, bien noté. Je suis là et disponible…` |
| `:8766` `{"question":"PONG","agent":"hermes"}` + jeton | **200 `ok:false` en 0,0 s** : `hermes indisponible` |

Réseau : ponts côté hôte Windows, joints depuis WSL via le proxy local
(`http_proxy` 127.0.0.1:56209) ; TCP direct WSL → 127.0.0.1 refusé, normal.
URLs `.env.local` (non secret) : `CODEX_BRIDGE_URL=http://host.docker.internal:8765/ask`,
`CLI_BRIDGE_URL=http://127.0.0.1:8766/ask`.

## Blockers

1. **Registre hostagent live non rejoué** : `numpy`/`pytest`/`httpx` absents du
   python WSL et `docker.sock` inaccessible (`permission denied`) → pas
   d'import `serve_hostagent`, pas de pytest ce soir. Preuve = enregistrement
   direct + lecture du code (l.102-173 : `ask_codex` si jeton + client,
   `ask_hermes` refusé).
2. **`.env.local` mixte CRLF/LF** (`file` : `with CRLF, LF line terminators`).
   Si le chargeur conteneur ne strippe pas, un `\r` pollue jeton/URL et le
   pont répond 401. À confirmer au prochain tour vocal.
3. Non-blocker, rappel : `ask_hermes` volontairement non exposé — pas un manque.

## Next smoke

1. Boot hostagent : attendre `OUTILS: ask_codex, …` (pas `aucun backend configure`).
2. PTT « Bonjour » (tour sans outil inchangé) puis question code → annonce
   « Je demande à Codex… » + réponse parlée, pas de panne audible.
3. Si 401 côté conteneur : normaliser `.env.local` en LF, re-smoke.
4. Re-PONG `:8765`/`:8766` avant chaque démo (5-11 s à chaud).
