---
date: 2026-09-19
heure: ~20:04 Europe/Paris
type: push
auteur: Cursor (Grok 4.6) — préparation, non exécuté
cible: OC
deadline: 20:50 Europe/Paris
branche: nuit/2026-08-27 @ 33b423c
statut: pret — commit/push OC
---

# Push 19 sept — runbook OC (pas encore poussé)

Branche : `nuit/2026-08-27` @ `33b423c`, à jour avec `origin`. Rien n'est commité.
Thomas absent. **OC exécute** les commandes ci-dessous. Sans `.env.local`.

Détail lanes : `[[2026-09-19-CURSOR-OUT]]`.

## Avant le `git add`

1. Relire `git status --short` : si `src/i18n.py` ou `src/mouth/remote_tts.py` sont apparus **et** leurs tests passent, on peut les ajouter. Sinon les tests restent exclus.
2. Si `nights/2026-09-19-C4-OUT.md` / `P0-1-SSD-E.md` / `P0-2-ENV.md` / `WIRE-OUT.md` existent, ils rentrent avec le glob `nights/2026-09-19-*.md`.

## Commandes (PowerShell)

```powershell
cd "D:\BGB Training\MOTHER-dev"

git add .gitignore .env.example requirements-extra.txt
git add dev/scripts/serve_hostagent.py dev/scripts/veille_hf.py dev/scripts/banc_oreille.py dev/scripts/carte_figee.env
git add dev/tests
git reset HEAD -- dev/tests/test_c8_i18n.py dev/tests/test_remote_tts.py
git add native src/brain src/ears src/mouth
git add workers/night_health_vault_note resources/bpmn/night_health_vault_note.bpmn
git add nights/2026-09-19-*.md nights/2026-09-19-c2-*.png nights/degustation-19

git status --short
```

Le `git reset` des deux tests C8/C5 est **obligatoire** tant que `src.i18n` et `src.mouth.remote_tts` manquent : sinon `pytest dev/tests` casse à la collecte.

## Vérifs après add, avant commit

```powershell
git diff --cached --name-only
git diff --cached --name-only | Select-String -Pattern '\.env\.local|carte-secrete|carte-reel|enregistrements/'
```

Attendu : **aucune** ligne. `.env.example` (clés vides) OK. `carte_figee.env` OK (modèles, pas de jetons).

```powershell
git check-ignore -v .env.local nights/degustation-19/oreille/enregistrements/phrase1.wav nights/degustation-19/cerveau/.carte-secrete.json
```

## Commit (OC)

```powershell
git commit -m "$(@'
Soir 19 sept : carte figee Granite+Whisper large-v3+Magpie Sofia, C1-C4 C9-C12, notes nights/
'@)"
```

## Push (OC)

```powershell
git push origin HEAD
```

Puis coller hash + URL ici, comme `[[2026-09-18-PUSH]]`.

## Message attendu

```
Soir 19 sept : carte figee Granite+Whisper large-v3+Magpie Sofia, C1-C4 C9-C12, notes nights/
```
