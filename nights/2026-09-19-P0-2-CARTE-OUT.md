---
date: 2026-09-19
heure: ~20:25 Europe/Paris
type: out
lane: P0-2
cible: Claude (lead technique) + OC (push 20:50)
auteur: Cursor (Grok 4.6)
related: ["[[2026-09-19-CARTE-FIGEE]]", "[[2026-09-19-CLAUDE-POUR-GROK-SUITE]]"]
deadline: 20:50 Europe/Paris
---

# P0-2 OUT — carte persistante + hotwords Whisper

Thomas : finir max ce soir. **Pas de commit** (accord Thomas). **Pas de relance** du host-agent live (stack dégustation encore debout). Aucune valeur de secret affichée.

## Cause racine (avant correctif)

La carte figée (Granite + Whisper large-v3 + Magpie Sofia CUDA) ne vivait que dans `/tmp/relance_hostagent.sh` et `/tmp/brain.sh`.

- `charger_env_local` (C1) **ignore** `MOUTH_*` / `EARS_*` / `BRAIN_*` / `MODEL` — voulu pendant la dégustation.
- `.env.local` et `relancer_routeur.sh` posaient encore **router / Qwen3 / Supertonic / MiniCPM**.
- Un reboot via l'ancien lanceur ou un `serve_hostagent.py` nu retombait sur Pocket/Estelle.

Hotwords Whisper (`MOTHER Codex Camunda Claude`) validés au banc, **absents** de `FasterWhisperASR._decode`.

## Diff résumé

| Fichier | Rôle |
|---|---|
| `dev/scripts/carte_figee.env` | Source de vérité **sans secret** |
| `dev/scripts/serve_hostagent.py` | `charger_carte_figee()` après C1 ; log `CARTE : …` ; `EARS_HOTWORDS` |
| `dev/scripts/relance_hostagent.sh` | Lanceur officiel (remplace `/tmp/relance_hostagent.sh`) |
| `dev/scripts/relancer_routeur.sh` | Délègue au lanceur officiel (plus de Qwen3/Supertonic) |
| `dev/scripts/serve_llama.sh` | Défaut Granite Q4_K_M, ctx 4096, `--no-mmap` |
| `src/ears/faster_whisper_asr.py` | `hotwords=` passé à `transcribe` |
| `.env.local` | Clés **modèle** alignées (gitignoré, pas de commit) |

`CARTE_FIGEE=0` désactive le chargeur. `MOUTH_DEVICE_FORCE=cpu` (etc.) écrase une clé.

## TDD rouge → vert

**Rouge** (fonction absente) :

```
$ python -m pytest -q dev/tests/test_carte_figee.py --tb=short
FFFFF                                                                    [100%]
AttributeError: module 'serve_hostagent_env_local' has no attribute 'charger_carte_figee'
5 failed in 0.34s
```

**Rouge** hotwords :

```
$ python -m pytest -q dev/tests/test_ears_hotwords.py --tb=short
F.F
TypeError: FasterWhisperASR.__init__() got an unexpected keyword argument 'hotwords'
2 failed, 1 passed in 0.16s
```

**Vert** :

```
$ python -m pytest -q dev/tests/test_carte_figee.py dev/tests/test_ears_hotwords.py
........                                                                 [100%]
8 passed in 0.14s
```

Dry-run chargeur (env de test, pas le process live) :

```
injectees: BRAIN_MODEL, BRAIN_MODEL_LOCAL, BRAIN_SERVICE, EARS_BACKEND,
  EARS_COMPUTE_TYPE, EARS_DEVICE, EARS_HOTWORDS, EARS_LANGUAGE, EARS_MODEL,
  MODEL, MOUTH_BACKEND, MOUTH_DEVICE, MOUTH_LANGUAGE, MOUTH_VOICE_NAME
BRAIN_SERVICE llamacpp
EARS faster-whisper large-v3 int8_float16
MOUTH magpie Sofia cuda
HOTWORDS MOTHER Codex Camunda Claude
MODEL granite-4.2-3b-Q4_K_M.gguf
secret leftover False
```

`.env.local` clés modèle (valeurs attendues, **ok**) : `BRAIN_SERVICE` `MODEL` `EARS_BACKEND` `EARS_MODEL` `EARS_COMPUTE_TYPE` `EARS_HOTWORDS` `MOUTH_BACKEND` `MOUTH_VOICE_NAME` `MOUTH_LANGUAGE` `MOUTH_DEVICE`. Jetons / `BRAIN_API_KEY` **non touchés**.

## Relance live — non faite

Le host-agent tourne encore via `/tmp/relance_hostagent.sh`. Relance officielle, **quand Claude donne le feu vert**, sans `docker compose up` :

```
docker exec mother-core-dev bash /workspace/dev/scripts/relance_hostagent.sh
```

Log boot attendu (clés seulement) :

```
CARTE : BRAIN_MODEL, BRAIN_MODEL_LOCAL, BRAIN_SERVICE, EARS_…, MOUTH_… depuis carte_figee.env
EARS  : chargement faster-whisper / large-v3 sur cuda hotwords=MOTHER Codex Camunda Claude…
BRAIN : llama.cpp @ http://localhost:8080/v1/chat/completions — HTTP 200
MOUTH : chargement magpie Sofia…
OUTILS: ask_claude, ask_codex, web_search — porte en mode auto
écoute sur 0.0.0.0:8001 /hostagent
```

(`calculer` peut apparaître si C12 est déjà branché — pas cette lane.)

## Non fait (hors P0-2 / temps)

- Relance live + preuve boot sur le process en cours
- Branchement JeV (C3 module prêt, pas câblé)
- C8 EN, C13 pythonw (cause non reprouvée ici)
- Commit / push (OC 20:50, Thomas valide)

## Done

| Attendu | Statut |
|---|---|
| Carte persistante hors `/tmp` | **oui** (`carte_figee.env` + chargeur + lanceur) |
| `.env.local` clés modèle | **oui** (gitignoré) |
| Hotwords EARS | **oui** |
| pytest rouge→vert collé | **oui** |
| Relance live | **non** — feu vert Claude |
| Commit | **non** |
